import { DeliverycustomerDto } from './deliverycustomer-dto';

describe('DeliverycustomerDto', () => {
  it('should create an instance', () => {
    expect(new DeliverycustomerDto()).toBeTruthy();
  });
});
